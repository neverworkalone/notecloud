version: 2

defaults: &defaults
  working_directory: ~/repos/wc
  docker:
    - image: vuejs/ci

jobs:
  build:
    <<: *defaults
    steps:
      - checkout
      - run: npm install
      - run: npm run build

workflows:
  version: 2
  build:
    jobs:
      - build
